import request from "@/utils/request";

const state = {
	shopDetail: {}
};
const mutations = {
	SET_SHOP_DETAIL(state,shopDetail){
		state.shopDetail = shopDetail;
	}
};
const actions = {
	
};
const getters = {
	
};
export default {
	state,
	mutations,
	actions,
	getters
};